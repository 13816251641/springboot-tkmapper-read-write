server:
    port: 8080
logging:
    level:
        com.lujieni.tkmapper.dao: debug

spring:
    datasource:
        master:
            username: root
            password: root
            url: jdbc:mysql://localhost:3306/springboot-tkmapper
            driver-class-name: com.mysql.jdbc.Driver
        slave:
            username: root
            password: root
            url: jdbc:mysql://localhost:3306/springboot-tkmapper-slave
            driver-class-name: com.mysql.jdbc.Driver
